# ☸️ Kubernetes Ingress - BikeMizer para Minikube
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bikemizer-ingress
  labels:
    app: bikemizer
  annotations:
    # Minikube specific annotations
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    # Rate limiting
    nginx.ingress.kubernetes.io/rate-limit: "100"
    nginx.ingress.kubernetes.io/rate-limit-window: "1m"
    # CORS
    nginx.ingress.kubernetes.io/enable-cors: "true"
    nginx.ingress.kubernetes.io/cors-allow-origin: "*"
    nginx.ingress.kubernetes.io/cors-allow-methods: "GET, POST, PUT, DELETE, OPTIONS"
    nginx.ingress.kubernetes.io/cors-allow-headers: "DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization"
spec:
  rules:
  - host: bikemizer.local  # Minikube local domain
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: bikemizer-service
            port:
              number: 80
  # Para usar com minikube tunnel
  # - host: bikemizer.example.com
  #   http:
  #     paths:
  #     - path: /
  #       pathType: Prefix
  #       backend:
  #         service:
  #           name: bikemizer-service
  #           port:
  #             number: 80
